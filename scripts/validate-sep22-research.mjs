import fs from 'node:fs';import ts from 'typescript';import vm from 'node:vm';
const path='app/research/sep22-research-records.ts';const source=fs.readFileSync(path,'utf8');const js=ts.transpileModule(source,{compilerOptions:{module:ts.ModuleKind.CommonJS,target:ts.ScriptTarget.ES2020}}).outputText;const module={exports:{}};vm.runInNewContext(`(function(exports,module){${js}})(module.exports,module)`,{module,exports:module.exports});const posts=module.exports.september22ResearchRecords;
if(posts.length!==5||new Set(posts.map(p=>p.slug)).size!==5)throw Error('expected exactly five unique articles');
for(const p of posts){const words=p.body.join(' ').match(/\b[\w’'-]+\b/g)?.length||0;if(words<1200)throw Error(`${p.slug}: ${words} words`);if(p.published!=='2026-09-22'||p.sources.some(s=>s.checked!=='2026-09-22'))throw Error(`${p.slug}: date mismatch`);if(p.sources.length<2||p.related.length!==3||!p.body.some(x=>x.startsWith('Limitations.')))throw Error(`${p.slug}: editorial fields missing`)}
const fleet=fs.readFileSync('app/fleet-content.ts','utf8');if(!fleet.includes('...september22ResearchRecords'))throw Error('batch missing from Research inventory');console.log('Validated exactly five unique September 22 Research articles; all exceed 1,200 words and have aligned dates, sources, limitations, and links.');
